import React from "react";
import logo from './logo.svg';

function Header() {
    // Import result is the URL of your image
    return <img width={300} height={200} src={logo} alt="Logo" />
}

export default Header;